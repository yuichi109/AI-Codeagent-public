# ドキュメント駆動型マルチエージェント設計書

> 作成: 2026-05-10  
> ステータス: 設計中・未実装  
> 優先度: 現行の改良・追加作業が一段落したら着手

---

## 基本方針：通常モードとは完全に分離

**チャット画面に「🤖 マルチエージェント」トグルボタンを設置し、明示的にONにした場合のみ動作する。**

- デフォルトはOFF（通常の単一エージェントモード）
- 重要なタスク・品質を特に求める場合にだけ使う
- 理由：複数モデルを複数ターン動かすためトークン消費が大幅に増える
- ONにしたまま雑談・軽いタスクに使うのは非推奨

---

## なぜこの機能が必要か

エンジニア仲間が同様の仕組みで、AIエージェントに高効率・高品質な成果物を作らせている実績がある。

単一のAIが全部やるより、**役割を分担した複数のAIが協調**することで：
- 各エージェントが自分の専門に集中できる
- 複数モデル（GPT-4.1-mini / GPT-4.1 / gpt-5-mini 等）を役割ごとに割り当てられる
- 並列実行により処理速度が上がる
- 一つのエージェントの見落としを別のエージェントが補える

---

## システム概要

```
ユーザーがチャットで指令
  └─ ディスパッチャー（タスク分解・割り当て）
       ├─ 設計エージェント
       │    └─ design.md を書く
       ├─ コーディングエージェント
       │    └─ code.py + how-to-use.md を書く
       ├─ ドキュメントエージェント
       │    └─ README等を書く
       └─ デバッグ/テストエージェント
            └─ test-result.md を書く

各エージェントは互いのドキュメントを読んで自分の仕事をする
進捗をステータスファイルに書き込む
閾値 or 納期を超えたら中間報告
```

---

## 核心的な設計方針

### エージェントは完全に独立・専門特化する

これはこのシステムの最重要原則。

- 各エージェントは**自分の責任範囲だけを実行する**
- 他のエージェントの出力を読むことはあっても、**修正・改善・手伝いは一切しない**
- 「ついでにここも直しておこう」は禁止。スコープ外は無視する
- エージェント間の仕事の取り合い・割り込みはない

**なぜこれが重要か：**  
一つのAIに全部任せると、どれほど優秀なモデルでも勝手に範囲を広げて変な方向に進むことがある。  
役割を厳格に切ることで各AIが専門に集中し、結果として全体の精度が上がる。  
また、何かがおかしくなったときに「どのエージェントの出力が原因か」を特定しやすくなる。

**実装上の意味：**
- 各エージェントのシステムプロンプトに「あなたの仕事はXのみ。それ以外は出力しないこと」を明記
- 読んで良いファイル・書いて良いファイルをエージェントごとに定義し、それ以外には触れさせない
- ディスパッチャーだけが全体を把握し、流れを制御する

---

### 通信手段はファイルのみ・会話は一切しない

**エージェント同士は会話しない。ドキュメントだけが唯一の成果・通信手段。**

- エージェントが残せるのはファイルだけ（コード・設計書・テスト結果・使い方等）
- 他のエージェントへのメッセージ送信・返答・質問は存在しない
- 他のエージェントの成果ファイルを読んで自分の仕事に活かすことはあるが、それだけ
- コーディングエージェントはコード本体と「このコードの使い方・意図」を両方ファイルとして残す
- 会話がないからこそ干渉が起きない・スコープが崩れない

**なぜ会話させないか（実体験からの教訓）：**

多くの実装者が「上司AI × 専任AI 8人」のような会話型マルチエージェントを試した結果、
無駄な遠回り・意味のないループ・トークン爆発が頻発することが分かっている。
AIは会話の中で「確認」「相談」「訂正」を繰り返し、本来の仕事より会話コストが膨らむ。

**成果物ファイルだけでやり取りする方が無駄がなく、結果として品質も上がる。**

実装時の注意：「エージェント間でやり取りさせた方が賢くなるのでは？」という誘惑に負けないこと。
それは罠であり、この設計の根幹を壊す。

この原則により：
- アーキテクチャがシンプルになる（リアルタイム通信の仕組みが不要）
- 全エージェントの仕事がファイルとして残るため、後から何をやったか追跡できる
- 人間がいつでも途中経過を確認・介入できる

### ループと完了判定・納期管理

全体の進捗を把握しているのはディスパッチャーだけなので、**ループ管理・催促・中間報告はすべてディスパッチャーが担う**。

**通常の完了フロー：**
- 各エージェントが完了したら status.md に完了を記録
- ディスパッチャーが全員の完了を確認したら最終報告をユーザーに返す
- 品質閾値を超えなければ該当エージェントを再実行してループ

**納期超過フロー：**
- ディスパッチャーが定期的に status.md を確認する
- 設定した制限時間を超えてもエージェントが完了していない場合、**ディスパッチャーから催促**を送る
  - 催促 = そのエージェントを再度呼び出し「残り時間Xです。現時点の成果を提出してください」と伝える
- 最終納期を過ぎたら完成・未完成にかかわらず**中間報告を生成してユーザーに返す**

**設定可能にするパラメータ：**
- 最大ループ回数
- エージェントごとのタイムアウト（催促トリガー）
- 全体の制限時間（中間報告トリガー）
- 品質閾値（何をもって「完了」とするか）

### モデルの割り当て

役割ごとに異なるモデルを割り当てられる：

| 役割 | 想定モデル |
|---|---|
| ディスパッチャー | gpt-5.4系（判断・分解が重要） |
| 設計 | gpt-5.4系（高品質・高性能を優先） |
| コーディング | gpt-5.4-mini系（速度重視） |
| インフラ | gpt-5.4系（判断精度が重要） |
| ドキュメント | gpt-5.4-mini系 |
| デバッグ/テスト | gpt-5.4系（品質重視） |

※ Foundry の複数インスタンス（FOUNDRY_2_* 等）も活用できる

---

## ファイル構成（実行時のworkspace例）

```
workspace/
  jobs/
    {job-id}/
      task.md          ← ディスパッチャーが書く（全体指令・役割分担）
      design.md        ← 設計エージェントの出力
      code/            ← コーディングエージェントの出力
        main.py
        how-to-use.md
      docs/            ← ドキュメントエージェントの出力
      test-result.md   ← デバッグエージェントの出力
      status.md        ← 各エージェントの進捗状態（共有）
      final-report.md  ← 完了 or 中間報告
```

---

## 実装フェーズ

### Phase 1: 逐次版（まず動かす）
- ディスパッチャーがタスクを分解し、各エージェントを**順番に**呼ぶ
- 現在の `call_responses_api` の発展形として実装可能
- UIは既存のチャット画面で進捗をテキスト表示

難しさ: ★★

### Phase 2: 並列版
- `asyncio.gather` で複数エージェントを同時実行
- UIに複数エージェントの進捗を並列表示（ストリーム）
- SSE or WebSocket での複数ストリーム管理

難しさ: ★★★★

### Phase 3: ループ・品質制御
- デバッグエージェントが品質を評価してループを制御
- 閾値・ループ上限・納期の設定UI
- 中間報告の自動生成

難しさ: ★★★

---

## 現行アーキテクチャとの関係

| 必要なもの | 現状 |
|---|---|
| ファイルの読み書き | `read_file` / `write_file` 済み ✅ |
| コマンド実行 | `run_command` 済み ✅ |
| 複数モデルへの委譲 | `call_responses_api` が原型 △ |
| 複数Foundryインスタンス | `FOUNDRY_2_*` 等で設定可能 ✅ |
| 並列実行 | 未実装 ❌ |
| ディスパッチャーロジック | 未実装 ❌ |
| ループ・閾値制御 | 未実装 ❌ |
| 複数ストリームのUI表示 | 未実装 ❌ |

---

## 関連する既存イシュー・機能

- **#27 ハイブリッドモード**: ローカルモデルをオーケストレーターにする案。本機能とは別軸だが設計思想は近い。
- **MAGIシステム（ロードマップ）**: 3つのLLMが議決する案。本機能の簡易版として位置づけ可能。
- **call_responses_api**: 現在の逐次サブエージェント。Phase 1の起点になる。

---

## 役割一覧（エージェント候補）

タスクの性質によってディスパッチャーが必要な役割を選んで動かす。全員を常に起動するわけではない。

| 役割 | 必須/任意 | 補足 |
|---|---|---|
| ディスパッチャー | 必須 | 常に1名。タスク分解・割り当て・進捗管理・催促・中間報告 |
| 設計 | 必須 | 高性能モデル1名。アーキテクチャ・既存コードとの整合性 |
| コーディング | 必須 | タスク次第で1〜複数。ファイル境界で分割できる場合のみ並列化 |
| インフラ | タスク次第 | 実環境が必要な場合のみ。コンテナ・クラウドリソース構築・動作確認・環境情報をファイルに残す |
| レビュー/デバッグ | 必須 | 実環境でのテスト・品質評価・ループ判定 |
| ドキュメント | 任意 | changelog・設計書・コメント整備。省略可能なことも |

**インフラ担当が必要な理由：**  
設計AIが「このアーキテクチャで」と決めても、実際に動く環境で検証されなければ空論になる。  
インフラAIが実際にコンテナ・クラウドを構築して動作確認することで、設計とコードが現実の環境で成立することを保証する。

**コーディングを複数名にする条件：**  
触るファイルが完全に別である場合のみ並列化可能。依存関係がある場合はディスパッチャーが逐次実行に切り替える。

---

## インフラ担当AIの段階設計

### 対象プラットフォームと用途の整理

| 版 | インフラ担当 | 対象ユーザー |
|---|---|---|
| Windows版 | なし（コード生成・ドキュメントまで） | 手軽にコード作成したい人 |
| WSL版 Phase 1 | インフラ Lite | 技術者・自分 |
| WSL版 Phase 2 | インフラ Full | 技術者・自分 |

### インフラ Lite（第一段階・WSL版）

ローカル完結。追加コストなし。

- **Docker**：既存のUbuntu + Azure CLI + Ansibleイメージを `docker run` で即起動。OSインストール不要で作業開始できる
- **Windows Sandbox**：Windows 11 Pro/Enterprise/Education 限定。メモリ・CPU制約やVM環境では厳しいためメインには据えない

### インフラ Full（第二段階・WSL版）

リモート環境への接続・プロビジョニングが可能。

**接続先プロファイルを登録して使い分ける設計：**

```
接続先プロファイル
  ├── Azure VM      → AIがプロビジョニングから実施（Azure CLI）
  ├── vSphere VM    → AIがAnsibleでOSデプロイから実施（既存プレイブック活用）
  └── 既存ホスト    → ユーザーがIPと管理IDを登録 → SSHで接続して作業
```

**vSphereについて：**
- Windows・Linuxをデプロイするためのコードが大量に既存で存在する
- `community.vmware` コレクション（既にAnsibleツールで対応済み）を活用
- Azure VMより早く使える可能性がある

**既存ホストについて：**
- 物理・仮想・クラウド問わず疎通可能であればどこでも使える
- ユーザーがOSを事前に用意してIPと管理IDを登録するだけ
- 最もシンプルで汎用性が高い

### 最終ビジョン（フルオートパイプライン）

```
ユーザー「Windowsで動く〇〇を作って」
  │
  ├── 設計AI        → 設計書を書く
  ├── コーディングAI → コードを書く
  │
  ├── インフラAI    → Azure or vSphere に
  │                   既存Ansibleプレイブックでテスト用OS環境をデプロイ
  │                   「デプロイ完了・IPは〇〇」をステータスファイルに記録
  │
  ├── テスターAI    → デプロイされた環境にSSH等で接続
  │                   実際にインストール・動作確認
  │                   「成功/失敗・ログ」をテスト結果ファイルに記録
  │
  └── ディスパッチャー → 全体を見て完了判定 or 差し戻し
```

ユーザーがやることは要件を言うだけ。設計・実装・環境構築・テストが全自動で完結する。

### Windowsテスト環境の選択フロー

```
Windowsをテスト対象にしたい
  │
  ├── Windows 11 Pro/Enterprise/Education
  │     かつ メモリ・CPU に余裕あり
  │     かつ VM内ネスト仮想化ではない
  │         → Windows Sandbox が使える（条件が揃えばラッキー程度）
  │
  ├── Windows 10 / Windows Server / リソース不足
  │     → Sandbox 使用不可
  │         ├── Windows版：TCP/IP接続できる別PCを用意してもらう
  │         └── WSL版：Azure等クラウドに自動プロビジョニング
  │
  └── どちらでも「TCP/IP接続可能なOS」があれば接続して使える
```

**設計方針：Windows Sandboxは「条件が揃えば使える」程度の位置づけ。メインの設計には据えない。**

### セットアップ時のUX方針：「使えない」で終わらない

非対応環境を検出しても詰まらせず、次の選択肢を順番に案内する。

```
非対応OS・Sandbox使用不可 と検出
  │
  ├── 「テスト用に別PCを用意できますか？」
  │     YES → IPと管理IDを登録 → テストまで自動でやってくれる
  │     NO  → 「クラウドのアカウントはありますか？」
  │             YES → 自分でVMを立てる手順を案内 → 登録 → テスト
  │             NO  → 「WSLを導入すると自動でVM構築できます」
  │                     → WSL + Ubuntuイメージの導入手順を案内
  │
  └── 社内・配下ユーザー向け
        → 構成済みUbuntuイメージ（Ansible・Docker入り）を適用するだけで即フルスタック
        → 「このイメージを入れてください」の一言で完結
```

**原則：「このOSでは使えません」だけで終わらせない。必ず次のアクションを示す。**

### WSL版の動作要件

WSL版はUbuntu前提で作られているため、WSLのディストリビューションは **Ubuntu（20.04 / 22.04 / 24.04）に限定する**。
Debian・Fedora・openSUSEでは動作しない部分がある（apt・systemd・bubblewrap等）。

---

## 未決定事項

- エージェントの役割セットはプロジェクトごとに変えるか、固定か
- 品質評価の方法（別エージェントに採点させる / ルールベース / 人間が閾値を設定）
- UIでの複数エージェント進捗の見せ方
- 失敗したエージェントのリトライ戦略
- **ディレクターとディスパッチャーを分けるか**
  - ディレクター：ユーザーの意図を深く理解し「何を・なぜ・どこまで」を決める。品質基準・完了条件・優先順位を定義。実装詳細には踏み込まない。
  - ディスパッチャー：ディレクターの方針を受けて「誰が・何を・いつまでに」に落とす。タスク割り当て・進捗監視・催促。
  - 分ける価値がある場面：タスクが複雑で戦略的判断と実務調整が両方重い場合。一つにまとめると判断の質が下がる可能性。
  - 分けない方が良い場面：タスクがシンプルで「何を作るか」が自明な場合。トークンと複雑さが増えるだけ。
  - 方針：最初はディスパッチャーに統合し、必要を感じたら分離する。
