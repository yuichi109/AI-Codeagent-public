{
  "servers": [
    {
      "id": "playwright",
      "name": "Playwright MCP",
      "transport": "stdio",
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless",
        "--browser",
        "msedge",
        "--output-dir",
        "./workspace/playwright-screenshots"
      ],
      "enabled": true
    },
    {
      "id": "obsidian",
      "name": "Obsidian MCP",
      "transport": "stdio",
      "command": "npx",
      "args": [
        "obsidian-mcp",
        "${OBSIDIAN_VAULT_PATH}"
      ],
      "timeout": 120,
      "idle_timeout": 60,
      "enabled": true
    }
  ]
}