services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_SECRET=ai-codeagent-secret
    volumes:
      - ./searxng-settings:/etc/searxng
    restart: unless-stopped
